# ASR 异步转写队列 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** dysync 字幕生成从「同步阻塞」改为「异步队列+扫库消费」，五态可视，容器重启自愈。

**Architecture:** ASR 侧（Python）队列落盘+文件式 submit+幂等键+队列非空不自停；dysync 侧（.NET）SubtitleQueueJob 每 2 分钟扫库提交/轮询/写回，同步任务删阻塞转写；前端五态+条件轮询。

**Tech Stack:** Python FastAPI（ASR-For-Dysync）、.NET 6 + Quartz（dy.net）、Vue3+AntD（app）。

**Spec:** `docs/superpowers/specs/2026-08-24-asr-queue-design.md`

## Global Constraints

- **ASR 项目**在 `D:/ASR-For-Dysync`（独立 git 仓，分支 master）。**改码后生效方式**：杀 python 进程 → 写 start.flag 让 watcher 拉起（自动加载新代码）。**便携包 D:\ASR-Portable 是另一份拷贝不会自动同步**（本计划不动便携包）
- **dysync**在 `D:/dysync/dysync.net`（分支 asr-windows-test）。编译：`D:/dotnet-sdk/dotnet.exe build D:/dysync/dysync.net/dy.net.csproj -c Release 2>&1 | grep -c " error "` 期望 0
- dysync 部署速查：publish(-r linux-x64 --self-contained false -o D:/dysync/build-context/pub) → `docker cp .../dy.net.dll dysync2026:/app/dy.net.dll` → `docker commit dysync2026 dysync:asr-local` → cwd D:/dysync `docker compose up -d --force-recreate`。**每步验 md5**（docker cp/commit 输出不截断）
- 前端构建 `cd D:/dysync/dysync.net/app && npm run build`；dist cp 后查嵌套（`ls /app/app/dist/dist` 存在则展平）
- 现有 ASR 契约：`GET /api/health`、`POST /api/transcribe`（同步）、`GET /api/asr/status?task_id`（0/1/2/3）、`GET /api/jobs/list`。`_run_asr(audio_path, display_name)` 返回 dict 含 text/segments/token_count/duration_ms
- ASR 轮询端点**不改**（dysync 轮询不读 result 字段）
- sqlite 检查统一 `PYTHONIOENCODING=utf-8 python -c "..."` 连 `D:/dysync/data/db/dy.sqlite`
- 每任务 git commit（各仓各自 cwd）

---

### Task 1: ASR 队列落盘 + 幂等键 + 文件式 submit

**Files:**
- Modify: `D:/ASR-For-Dysync/asr_jobs.py`（全文件重构，现有 submit(url) 契约保留给 live-platform）
- Modify: `D:/ASR-For-Dysync/app.py`（新端点 + worker 处理器改造 + idle 判定 + tmp 清扫）

**Interfaces:**
- Produces（Task 3 dysync 消费）:
  - `POST /api/transcribe/submit`（multipart: file, title, source, idempotency_key）→ `{"task_id": int, "deduplicated": bool}`
  - `GET /api/asr/status?task_id=` 不变
  - `GET /api/jobs/list` 响应新增 `doing: int`
- Produces（app.py 内部）: `asr_jobs.submit_file(path, title, source, idempotency_key) -> (task_id, dedup: bool)`、`asr_jobs.pending_count() -> int`、`asr_jobs.start_worker()` 不变

- [ ] **Step 1: asr_jobs.py 改造**

完整新内容（保留原文件头注释，更新设计要点说明）：

```python
# -*- coding: utf-8 -*-
"""
ASR 异步任务层(腾讯 CreateRecTask/DescribeTaskStatus 契约 + dysync 文件队列)

两类提交入口:
  submit(url)          URL 下载模式(live-platform 兼容,不落盘恢复——来源方可重提)
  submit_file(path)    本地文件模式(dysync 队列,落盘 data/queue.json 可恢复)

队列持久化: submit_file 的 pending(0/1) 任务写 queue.json(原子写);
启动时恢复——status=1(进程死亡时必然中断)重置为 0 重新入队。
幂等: idempotency_key 查重返回原 task_id(dedup=True)。

status: 0=waiting 1=doing 2=success 3=failed
"""
import json
import os
import queue
import threading
import time
from pathlib import Path
from typing import Callable, Optional

_jobs: "dict[int, dict]" = {}
_jobs_lock = threading.Lock()
_idem_index: "dict[str, int]" = {}          # idempotency_key -> task_id
_task_queue: "queue.Queue[int]" = queue.Queue()
_processor: Optional[Callable[[str], "tuple[str, list[dict]]"]] = None

_seq = 0
_seq_lock = threading.Lock()

DATA_DIR = Path(__file__).parent / "data"
QUEUE_FILE = DATA_DIR / "queue.json"
_TTL_SECONDS = 2 * 60 * 60


def _next_id() -> int:
    global _seq
    with _seq_lock:
        _seq += 1
        return _seq


def _save_queue_locked():
    """把 pending(0/1) 任务落盘(原子写)。调用方必须已持 _jobs_lock。"""
    pending = {tid: {
        "file_path": j.get("file_path", ""),
        "url": j.get("url", ""),
        "title": j.get("title", ""),
        "source": j.get("source", ""),
        "idempotency_key": j.get("idempotency_key", ""),
        "status": 0,   # 落盘一律按 0(重启后重跑)
    } for tid, j in _jobs.items() if j["status"] in (0, 1) and j.get("file_path")}
    DATA_DIR.mkdir(exist_ok=True)
    tmp = QUEUE_FILE.with_suffix(".tmp")
    tmp.write_text(json.dumps(pending), encoding="utf-8")
    tmp.replace(QUEUE_FILE)


def _load_queue_and_requeue():
    """启动恢复:读 queue.json,重建任务并重新入队。"""
    if not QUEUE_FILE.exists():
        return
    try:
        saved = json.loads(QUEUE_FILE.read_text(encoding="utf-8"))
    except Exception:
        return
    with _jobs_lock:
        for tid_s, j in saved.items():
            tid = int(tid_s)
            path = j.get("file_path", "")
            if not path or not os.path.exists(path):
                continue   # 临时文件已清(异常退出残留),放弃该任务(dysync 会 404 重报)
            _jobs[tid] = {
                "status": 0, "result": "", "result_detail": [], "error_msg": "",
                "created_at": time.time(), "url": "",
                "file_path": path, "title": j.get("title", ""),
                "source": j.get("source", ""),
                "idempotency_key": j.get("idempotency_key", ""),
            }
            if j.get("idempotency_key"):
                _idem_index[j["idempotency_key"]] = tid
            _task_queue.put(tid)
        # 恢复后立刻快照(丢弃已被跳过的)
        _save_queue_locked()


def set_processor(fn):
    global _processor
    _processor = fn


def submit(url: str) -> int:
    """URL 模式(live-platform):不入落盘队列。"""
    task_id = _next_id()
    with _jobs_lock:
        _jobs[task_id] = {
            "status": 0, "result": "", "result_detail": [], "error_msg": "",
            "created_at": time.time(), "url": url,
            "file_path": "", "title": "", "source": "live-platform", "idempotency_key": "",
        }
    _task_queue.put(task_id)
    return task_id


def submit_file(file_path: str, title: str = "", source: str = "dysync-queue",
                idempotency_key: str = "") -> "tuple[int, bool]":
    """文件模式(dysync):幂等+落盘。返回 (task_id, deduplicated)。"""
    with _jobs_lock:
        if idempotency_key and idempotency_key in _idem_index:
            old_tid = _idem_index[idempotency_key]
            j = _jobs.get(old_tid)
            if j and j["status"] in (0, 1, 2):   # 2 也返回原任务(结果可重取)
                return old_tid, True
            # 旧任务失败(3)或被 GC:走新建
        task_id = _next_id()
        _jobs[task_id] = {
            "status": 0, "result": "", "result_detail": [], "error_msg": "",
            "created_at": time.time(), "url": "",
            "file_path": file_path, "title": title, "source": source,
            "idempotency_key": idempotency_key,
        }
        if idempotency_key:
            _idem_index[idempotency_key] = task_id
        _save_queue_locked()
    _task_queue.put(task_id)
    return task_id, False


def get_status(task_id: int) -> Optional[dict]:
    with _jobs_lock:
        j = _jobs.get(int(task_id))
        if not j:
            return None
        return {
            "status": j["status"], "result": j["result"],
            "result_detail": j["result_detail"], "error_msg": j["error_msg"],
        }


def pending_count() -> int:
    """队列中未完成任务数(0/1)。"""
    with _jobs_lock:
        return sum(1 for j in _jobs.values() if j["status"] in (0, 1))


def start_worker() -> None:
    _load_queue_and_requeue()
    t = threading.Thread(target=_worker_loop, name="asr-jobs-worker", daemon=True)
    t.start()


def _worker_loop() -> None:
    while True:
        task_id = _task_queue.get()
        try:
            _process(task_id)
        except Exception as e:
            _mark_failed(task_id, f"worker error: {e}")
        finally:
            _task_queue.task_done()
            _gc()


def _process(task_id: int) -> None:
    with _jobs_lock:
        j = _jobs.get(task_id)
        if not j:
            return
        j["status"] = 1
        file_path = j.get("file_path") or ""
        url = j.get("url") or ""
    if _processor is None:
        _mark_failed(task_id, "processor not registered")
        return
    try:
        src = file_path or url
        text, detail = _processor(src)
        with _jobs_lock:
            j = _jobs.get(task_id)
            if j:
                j["status"] = 2
                j["result"] = text or ""
                j["result_detail"] = detail or []
                _save_queue_locked()   # 终态触发落盘(把该任务从 pending 移除)
    except Exception as e:
        _mark_failed(task_id, str(e))


def _mark_failed(task_id: int, err: str) -> None:
    with _jobs_lock:
        j = _jobs.get(task_id)
        if j:
            j["status"] = 3
            j["error_msg"] = (err or "")[:1000]
            _save_queue_locked()


def _gc() -> None:
    cutoff = time.time() - _TTL_SECONDS
    with _jobs_lock:
        stale = [tid for tid, j in _jobs.items() if j.get("created_at", 0) < cutoff]
        for tid in stale:
            j = _jobs.pop(tid, None)
            if j and j.get("idempotency_key"):
                _idem_index.pop(j["idempotency_key"], None)
```

- [ ] **Step 2: app.py——_process_asr_job 改造(本地文件不再下载)**

`_process_asr_job(url)` 整体替换为（去掉 urllib 下载段，视频抽音频逻辑不变）：

```python
def _process_asr_job(src: str):
    """异步任务处理器:src 为本地文件路径(dysync 队列)或 URL(live-platform)。

    Returns: (text, detail_list);异常由 asr_jobs 捕获标 status=3。
    """
    path = Path(src)
    suffix = path.suffix.lower() or ".wav"
    _fname = path.name[:60]
    audio_path = str(path)
    extracted = False
    try:
        if suffix in VIDEO_EXTS:
            audio_path = extract_audio(str(path))
            extracted = True
        res = _run_asr(audio_path, display_name=_fname)
        detail = [
            {"FinalSentence": s["text"], "StartMs": int(s["start"]), "EndMs": int(s["end"])}
            for s in res.get("segments", [])
        ]
        return res.get("text", ""), detail
    finally:
        if extracted:
            try:
                os.remove(audio_path)
            except OSError:
                pass
```

（如原函数尾部的 detail 构造/return 与此不同，以本代码为准整段替换）

- [ ] **Step 3: app.py——新端点 /api/transcribe/submit + tmp 清扫**

`asr_submit` 端点后追加：

```python
TMP_DIR = DATA_DIR_TMP = Path(__file__).parent / "data" / "tmp"
TMP_DIR.mkdir(parents=True, exist_ok=True)


def _sweep_tmp():
    """启动清扫临时目录残留(异常退出遗留)。"""
    for p in TMP_DIR.glob("*"):
        try:
            p.unlink()
        except OSError:
            pass


_sweep_tmp()


@app.post("/api/transcribe/submit")
async def transcribe_submit(
    file: UploadFile = File(...),
    title: str = Form(""),
    source: str = Form("dysync-queue"),
    idempotency_key: str = Form(""),
):
    """文件上传式异步提交(dysync 队列):存临时文件入队,立即返回 task_id。"""
    if MODEL is None:
        raise HTTPException(status_code=500, detail="模型未加载")
    suffix = Path(file.filename or "").suffix.lower() or ".wav"
    tmp = TMP_DIR / f"{time.time_ns()}_{Path(file.filename or 'f').name}"
    content = await file.read()
    tmp.write_bytes(content)
    task_id, dedup = asr_jobs.submit_file(str(tmp), title, source, idempotency_key)
    return {"task_id": task_id, "deduplicated": dedup}
```

worker 处理完的临时文件清理：`asr_jobs._process` 成功/失败后删 `file_path`——在 app.py 无法插入，改在 `_process_asr_job` 的 finally 加（本地文件模式才删，URL 模式跳过）：

```python
    finally:
        if extracted:
            try:
                os.remove(audio_path)
            except OSError:
                pass
        # dysync 队列的源临时文件:处理完即删(成功失败都删)
        if str(path).startswith(str(TMP_DIR)):
            try:
                os.remove(str(path))
            except OSError:
                pass
```

（`from fastapi import Form` 如未导入需补——检查现有 import 行）

- [ ] **Step 4: app.py——idle 判定加队列条件**

`_idle_watchdog` 中 `if idle > IDLE_EXIT_MINUTES * 60:` 改为：

```python
        if idle > IDLE_EXIT_MINUTES * 60 and asr_jobs.pending_count() == 0:
```

（队列非空时永不自停——dysync 的请求不算持续心跳，靠此条件保活）

- [ ] **Step 5: app.py——jobs/list 加 doing 计数**

`/api/jobs/list` 端点（约 400-412 行）`queued` 计算后追加：

```python
    doing = sum(1 for j in asr_jobs._jobs.values() if j["status"] == 1)
```

并加入 `job_store.list_jobs(...)` 调用或响应字段（看现有结构——`result["doing"] = doing` 或并入 dict）。

- [ ] **Step 6: 语法验证**

```bash
cd D:/ASR-For-Dysync && python -m py_compile app.py asr_jobs.py && echo OK
```

- [ ] **Step 7: 重载 ASR + 冒烟**

```bash
powershell -NoProfile -Command "Get-Process python -ErrorAction SilentlyContinue | Stop-Process -Force"
echo flag > D:/dysync/asr-bridge/start.flag
# 等 30s 拉起
sleep 35
curl -s http://127.0.0.1:8000/api/health | head -c 120
# submit 冒烟(拿一个已存在的 srt 对应视频文件,幂等两次):
F=$(ls D:/dysync/data/uper/技术爬爬虾/*/*.mp4 2>/dev/null | head -1)
curl -s -X POST http://127.0.0.1:8000/api/transcribe/submit -F "file=@$F" -F "title=队列冒烟" -F "idempotency_key=smoke1"
curl -s -X POST http://127.0.0.1:8000/api/transcribe/submit -F "file=@$F" -F "title=队列冒烟" -F "idempotency_key=smoke1"
# 期望两次返回相同 task_id,第二次 deduplicated=true
```

- [ ] **Step 8: Commit（ASR 仓）**

```bash
cd D:/ASR-For-Dysync && git add -A && git commit -m "feat: 队列落盘恢复+文件式submit幂等+队列非空不自停+doing计数"
```

---

### Task 2: dysync 实体新列 + ASR 队列客户端

**Files:**
- Modify: `D:/dysync/dysync.net/model/entity/DouyinVideo.cs`（SubtitleStatusMsg 后加 3 属性）
- Modify: `D:/dysync/dysync.net/service/LocalAsrSubtitleService.cs`（新增队列客户端方法区）

**Interfaces:**
- Produces: `DouyinVideo.AsrTaskId(long?)/AsrTaskStatus(int?)/AsrRetryCount(int)`
- Produces（Task 3 Job 消费）: `LocalAsrSubtitleService` 新公有方法：
  - `Task<(bool Ok, long TaskId, bool Dedup)> QueueSubmitAsync(AppConfig config, string videoPath, string title, string idempotencyKey, CancellationToken ct)`
  - `Task<(int Status, string Text, List<AsrSegment> Segments, string Error)> QueueStatusAsync(AppConfig config, long taskId, CancellationToken ct)`（Status=-1 表 404）

- [ ] **Step 1: DouyinVideo 3 属性**（SubtitleStatusMsg 后）

```csharp
        /// <summary>ASR异步队列任务id(null=未提交)</summary>
        [SugarColumn(IsNullable = true)]
        public long? AsrTaskId { get; set; }
        /// <summary>ASR队列镜像状态:0排队/1转换中(终态不存)</summary>
        [SugarColumn(IsNullable = true)]
        public int? AsrTaskStatus { get; set; }
        /// <summary>队列任务404重报次数,≥3置失败</summary>
        [SugarColumn(IsNullable = true)]
        public int AsrRetryCount { get; set; }
```

- [ ] **Step 2: LocalAsrSubtitleService 加队列客户端**

类内新增（复用现有 `_httpClientFactory`/`ASR_HTTP_CLIENT`/`BuildEndpoint`/`AsrSegment`——AsrSegment 是现有嵌套类，含 Text/StartMs/EndMs）：

```csharp
        // ==================== 异步队列客户端(SubtitleQueueJob 用) ====================

        /// <summary>上传文件到 ASR 异步队列(幂等键防重复转写)。返回(task_id, 是否去重命中)。</summary>
        public async Task<(bool Ok, long TaskId, bool Dedup, string Error)> QueueSubmitAsync(
            AppConfig config, string videoPath, string title, string idempotencyKey,
            CancellationToken ct = default)
        {
            try
            {
                var validation = ValidateConfig(config);
                if (!validation.Success) return (false, 0, false, validation.Message);
                var client = _httpClientFactory.CreateClient(ASR_HTTP_CLIENT);

                await using var fs = new FileStream(videoPath, FileMode.Open, FileAccess.Read, FileShare.Read, 81920, true);
                using var fc = new StreamContent(fs);
                fc.Headers.ContentType = new System.Net.Http.Headers.MediaTypeHeaderValue("application/octet-stream");
                using var form = new MultipartFormDataContent();
                form.Add(fc, "file", Path.GetFileName(videoPath));
                form.Add(new StringContent(title ?? string.Empty), "title");
                form.Add(new StringContent("dysync-queue"), "source");
                form.Add(new StringContent(idempotencyKey ?? string.Empty), "idempotency_key");

                var resp = await client.PostAsync(BuildEndpoint(validation.ServiceUrl, "/api/transcribe/submit"), form, ct);
                var body = await resp.Content.ReadFromJsonAsync<AsrSubmitResp>(cancellationToken: ct);
                if (!resp.IsSuccessStatusCode || body == null || body.TaskId == 0)
                    return (false, 0, false, $"submit失败: HTTP {(int)resp.StatusCode}");
                return (true, body.TaskId, body.Deduplicated, string.Empty);
            }
            catch (Exception ex)
            {
                return (false, 0, false, ex.Message);
            }
        }

        /// <summary>查询队列任务状态。Status=-1 表示 404(任务丢失/GC)。成功时带全文与分段。</summary>
        public async Task<(int Status, string Text, List<AsrSegment> Segments, string Error)> QueueStatusAsync(
            AppConfig config, long taskId, CancellationToken ct = default)
        {
            try
            {
                var validation = ValidateConfig(config);
                if (!validation.Success) return (-2, null, null, validation.Message);
                var client = _httpClientFactory.CreateClient(ASR_HTTP_CLIENT);
                var resp = await client.GetAsync($"{validation.ServiceUrl}/api/asr/status?task_id={taskId}", ct);
                if ((int)resp.StatusCode == 404) return (-1, null, null, "task not found");
                var body = await resp.Content.ReadFromJsonAsync<AsrQueueStatusResp>(cancellationToken: ct);
                if (body == null) return (-2, null, null, "响应解析失败");
                var segs = body.ResultDetail?.Select(d => new AsrSegment
                {
                    Text = d.FinalSentence,
                    StartMs = d.StartMs,
                    EndMs = d.EndMs,
                }).ToList() ?? new List<AsrSegment>();
                return (body.Status, body.Result ?? string.Empty, segs, body.ErrorMsg ?? string.Empty);
            }
            catch (Exception ex)
            {
                return (-2, null, null, ex.Message);
            }
        }

        private sealed class AsrSubmitResp
        {
            [System.Text.Json.Serialization.JsonPropertyName("task_id")] public long TaskId { get; set; }
            [System.Text.Json.Serialization.JsonPropertyName("deduplicated")] public bool Deduplicated { get; set; }
        }
        private sealed class AsrQueueStatusResp
        {
            [System.Text.Json.Serialization.JsonPropertyName("status")] public int Status { get; set; }
            [System.Text.Json.Serialization.JsonPropertyName("result")] public string Result { get; set; }
            [System.Text.Json.Serialization.JsonPropertyName("result_detail")] public List<AsrQueueDetail> ResultDetail { get; set; }
            [System.Text.Json.Serialization.JsonPropertyName("error_msg")] public string ErrorMsg { get; set; }
        }
        private sealed class AsrQueueDetail
        {
            [System.Text.Json.Serialization.JsonPropertyName("FinalSentence")] public string FinalSentence { get; set; }
            [System.Text.Json.Serialization.JsonPropertyName("StartMs")] public long StartMs { get; set; }
            [System.Text.Json.Serialization.JsonPropertyName("EndMs")] public long EndMs { get; set; }
        }
```

注意：`AsrSegment` 现有嵌套类的属性名以文件实际为准（grep `class AsrSegment` 核对 Text/StartMs/EndMs 字段名，不匹配则适配）；`using System.Net.Http.Json;` 文件已有。

- [ ] **Step 3: 编译**（期望 0 error）→ **Step 4: Commit**

```bash
cd D:/dysync/dysync.net && git add -A && git commit -m "feat(asr): 实体队列3列+ASR异步队列客户端(submit/status)"
```

---

### Task 3: SubtitleQueueJob + 同步瘦身 + 注册

**Files:**
- Create: `D:/dysync/dysync.net/job/SubtitleQueueJob.cs`
- Modify: `D:/dysync/dysync.net/job/DouyinBasicSyncJob.cs`（SaveVideos 删字幕调用）
- Modify: `D:/dysync/dysync.net/service/DouyinQuartzJobService.cs`、`Program.cs`、`extension/ServiceExtension.cs`（注册）

**Interfaces:**
- Consumes: Task 2 的 QueueSubmitAsync/QueueStatusAsync、现有 EnsureAsrRunningAsync/CheckHealthAsync、BuildSrtContent（private static？grep 确认，若 private 需改 internal 或在 Job 侧重建——**先查**：`grep -n "BuildSrtContent" service/LocalAsrSubtitleService.cs`，若是 private static 改为 public static）
- Consumes: 现有 `douyinVideoService.GetAllAsync()/UpdateOne(video)`、`UpdateVideoSubtitleStateAsync`（private——Job 直接 `video.SubtitleSavePath=...; await UpdateOne(video)` 自写，不复用）

- [ ] **Step 1: BuildSrtContent 可见性**（若 private 改 public static，签名不变）

- [ ] **Step 2: SubtitleQueueJob.cs（新文件）**

```csharp
using dy.net.model.dto;
using dy.net.model.entity;
using dy.net.service;
using dy.net.utils;
using Quartz;

namespace dy.net.job
{
    /// <summary>字幕队列消费:每2分钟扫库——未提交的上传ASR异步队列,已提交的轮询写回。
    /// 404重报≤3次;手动先完成的让位;48h窗口+单轮100条。</summary>
    [DisallowConcurrentExecution]
    public class SubtitleQueueJob : IJob
    {
        private readonly DouyinCommonService commonService;
        private readonly DouyinVideoService douyinVideoService;
        private readonly LocalAsrSubtitleService asrService;

        public SubtitleQueueJob(DouyinCommonService commonService,
            DouyinVideoService douyinVideoService, LocalAsrSubtitleService asrService)
        {
            this.commonService = commonService;
            this.douyinVideoService = douyinVideoService;
            this.asrService = asrService;
        }

        public async Task Execute(IJobExecutionContext context)
        {
            var config = commonService.GetConfig();
            if (config == null || !config.AutoGenSubtitle) return;

            // ASR 健康检查(不通则走现有拉起流程;仍不通下轮再试)
            var health = await asrService.CheckHealthAsync(config);
            if (!health.Success)
            {
                var ensured = await asrService.CheckHealthAsync(config); // EnsureAsrRunningAsync 为 private 时用双检:拉起由 dysync 侧现有入口触发;这里只探
                Serilog.Log.Debug("[subtitle-queue] ASR未就绪: {Msg}", health.Message);
                return;
            }

            var all = await douyinVideoService.GetAllAsync();

            // ---- ① 轮询已提交的 ----
            var submitted = all.Where(v => v.AsrTaskId.HasValue
                && string.IsNullOrWhiteSpace(v.SubtitleSavePath)).ToList();
            foreach (var v in submitted)
            {
                var (status, text, segs, err) = await asrService.QueueStatusAsync(config, v.AsrTaskId.Value);
                switch (status)
                {
                    case 0:
                    case 1:
                        if (v.AsrTaskStatus != status) { v.AsrTaskStatus = status; await douyinVideoService.UpdateOne(v); }
                        break;
                    case 2: // 成功:写回(手动先完成则让位)
                        if (!string.IsNullOrWhiteSpace(v.SubtitleSavePath)) { v.AsrTaskId = null; v.AsrTaskStatus = null; await douyinVideoService.UpdateOne(v); break; }
                        var srt = LocalAsrSubtitleService.BuildSrtContentFrom(text, segs);
                        var srtPath = Path.ChangeExtension(v.VideoSavePath, ".srt");
                        var txtPath = Path.ChangeExtension(v.VideoSavePath, ".txt");
                        try
                        {
                            await File.WriteAllTextAsync(srtPath, srt, System.Text.Encoding.UTF8);
                            if (!string.IsNullOrWhiteSpace(text)) await File.WriteAllTextAsync(txtPath, text, System.Text.Encoding.UTF8);
                            v.SubtitleSavePath = srtPath;
                            v.SubtitleStatusMsg = "Subtitle generated via ASR queue.";
                            v.SubtitleCreateTime = DateTime.Now;
                            v.AsrTaskId = null; v.AsrTaskStatus = null; v.AsrRetryCount = 0;
                            await douyinVideoService.UpdateOne(v);
                        }
                        catch (Exception ex)
                        {
                            v.SubtitleStatusMsg = $"写回失败: {ex.Message}";
                            await douyinVideoService.UpdateOne(v);
                        }
                        break;
                    case 3:
                        v.SubtitleStatusMsg = $"ASR: {err}";
                        v.AsrTaskId = null; v.AsrTaskStatus = null;
                        await douyinVideoService.UpdateOne(v);
                        break;
                    case -1: // 404 任务丢失
                        v.AsrRetryCount++;
                        if (v.AsrRetryCount >= 3)
                        {
                            v.SubtitleStatusMsg = "ASR任务丢失,重试超限";
                            v.AsrTaskId = null; v.AsrTaskStatus = null;
                        }
                        else { v.AsrTaskId = null; v.AsrTaskStatus = null; } // 下轮重新提交
                        await douyinVideoService.UpdateOne(v);
                        break;
                    default: // -2 查询异常:不动,下轮再查
                        break;
                }
            }

            // ---- ② 提交新的(48h窗口,限100) ----
            var cutoff = DateTime.Now.AddHours(-48);
            var toSubmit = all.Where(v => string.IsNullOrWhiteSpace(v.SubtitleSavePath)
                && string.IsNullOrWhiteSpace(v.SubtitleStatusMsg)
                && !v.AsrTaskId.HasValue
                && v.SyncTime >= cutoff
                && !string.IsNullOrWhiteSpace(v.VideoSavePath))
                .Take(100).ToList();
            foreach (var v in toSubmit)
            {
                if (!File.Exists(v.VideoSavePath))
                {
                    v.SubtitleStatusMsg = "Video file not found.";
                    await douyinVideoService.UpdateOne(v);
                    continue;
                }
                var (ok, taskId, dedup, err) = await asrService.QueueSubmitAsync(
                    config, v.VideoSavePath, v.VideoTitle, $"dysync-{v.Id}");
                if (ok)
                {
                    v.AsrTaskId = taskId;
                    v.AsrTaskStatus = 0;
                    await douyinVideoService.UpdateOne(v);
                }
                else
                {
                    Serilog.Log.Debug("[subtitle-queue] 提交失败 {Id}: {Err}", v.Id, err);
                    break; // ASR不可用,本轮放弃剩余
                }
            }
        }
    }
}
```

注意 `BuildSrtContentFrom(text, segs)`——若 Task 3 Step 1 中 BuildSrtContent 签名是 `(AsrTranscribePayload)` 则在 LocalAsrSubtitleService 加一个适配重载：

```csharp
        /// <summary>队列结果→SRT(适配异步队列的 text+segments 形态)。</summary>
        public static string BuildSrtContentFrom(string text, List<AsrSegment> segments)
        {
            var payload = new AsrTranscribePayload { Text = text, Segments = segments };
            return BuildSrtContent(payload);
        }
```

（AsrTranscribePayload/AsrSegment 均为现有嵌套类；StartMs/EndMs 字段名以实际为准）

- [ ] **Step 3: SaveVideos 瘦身**

`DouyinBasicSyncJob.SaveVideos` 中删除：

```csharp
                if (config.AutoGenSubtitle)
                {
                    var (successCount, failedCount) = await localAsrSubtitleService.GenerateSubtitlesForVideosAsync(videos, config);
                    Log.Debug($"[{cookie.UserName}][{VideoType.GetDesc()}]-本地字幕生成完成，成功:{successCount}，失败:{failedCount}");
                }
```

替换为：

```csharp
                Log.Debug($"[{cookie.UserName}][{VideoType.GetDesc()}]-入库{videos.Count}条(字幕由队列处理)");
```

- [ ] **Step 4: 注册**（DouyinQuartzJobService/Program/ServiceExtension 三处，仿 stats.backfill 模式）

```csharp
        /// <summary>字幕队列消费任务(每2分钟;AutoGenSubtitle 关闭时 Job 内部自空转)。</summary>
        public async Task InitSubtitleQueueJob()
        {
            var scheduler = await _schedulerFactory.GetScheduler();
            var jobKey = new JobKey("subtitle.queue.job.key", DefaultJobGroup);
            var triggerKey = new TriggerKey("subtitle.queue.trigger.key", DefaultJobGroup);
            if (await scheduler.CheckExists(jobKey))
                await scheduler.DeleteJob(jobKey);
            var jobDetail = JobBuilder.Create<SubtitleQueueJob>()
                .WithIdentity(jobKey).WithDescription("字幕队列消费").DisallowConcurrentExecution().Build();
            var trigger = TriggerBuilder.Create()
                .WithIdentity(triggerKey).WithCronSchedule("0 */2 * * * ?").Build();
            await scheduler.ScheduleJob(jobDetail, trigger);
            Log.Information("【quartz】字幕队列任务已调度,cron=0 */2 * * * ?");
        }
```

Program.cs：`await backfillQuartzService.InitVideoStatsBackfillJob();` 后加

```csharp
                    await backfillQuartzService.InitSubtitleQueueJob();
```

ServiceExtension：`services.AddScoped<VideoStatsBackfillJob>();` 后加 `services.AddScoped<SubtitleQueueJob>();`

- [ ] **Step 5: 编译**（0 error）→ **Step 6: 部署 + 注册验证**（MD5 校验流程；`docker logs | grep 字幕队列`；QRTZ_CRON_TRIGGERS 出现 subtitle.queue 行）

- [ ] **Step 7: Commit**

```bash
cd D:/dysync/dysync.net && git add -A && git commit -m "feat(asr): SubtitleQueueJob扫库消费+同步瘦身+注册"
```

---

### Task 4: 前端五态 + ASR 看板队列格

**Files:**
- Modify: `D:/dysync/dysync.net/app/src/pages/workplace/RecordTable.vue`（subtitleStatusOf + 状态列模板 + 条件轮询）
- Modify: 列表 API 透出 asrTaskId/asrTaskStatus（grep `subtitleSavePath` 在 api/store 的映射处，加两字段）
- Modify: `D:/ASR-For-Dysync/static/index.html`（看板统计行加 队列/转换中 格——jobs/list 的 queued/doing 字段）

**Interfaces:**
- Consumes: Task 2 的实体字段（经列表接口透出）、Task 1 的 jobs/list doing 字段

- [ ] **Step 1: RecordTable 五态**

`subtitleStatusOf` 替换为：

```typescript
const subtitleStatusOf = (record: DataItem): 'unprocessed' | 'queued' | 'processing' | 'done' | 'error' => {
  if (generatingId.value && generatingId.value === record.id) return 'processing';
  if (record.subtitleSavePath) return 'done';
  if (record.subtitleStatusMsg) return 'error';
  if (record.asrTaskStatus === 1) return 'processing';
  if (record.asrTaskStatus === 0) return 'queued';
  return 'unprocessed';
};
```

状态列模板对应改（queued 橙色 tag「排队中」，processing 蓝「转换中」；DataItem 类型加 asrTaskId?: number; asrTaskStatus?: number）。

- [ ] **Step 2: 条件轮询**

```typescript
// 当前页存在非终态(queued/processing/unprocessed且今日)时 30s 刷新
let listTimer: number | undefined;
const armAutoRefresh = () => {
  window.clearInterval(listTimer);
  const hasPending = tableData.value.some(r => ['queued', 'processing'].includes(subtitleStatusOf(r)));
  if (hasPending) listTimer = window.setInterval(() => { loadTableData(); }, 30000);
};
```

`loadTableData` 完成后调 `armAutoRefresh()`；组件卸载 clearInterval（查现有 onUnmounted）。

- [ ] **Step 3: 列表字段透出**（后端 listing 响应映射处加 asrTaskId/asrTaskStatus——grep VideoController 列表端点返回结构，若直接序列化实体则零改动）

- [ ] **Step 4: ASR 看板队列格**（static/index.html 统计区，refreshJobs 已 3s 轮询；queued/doing 字段渲染成两个格子，样式仿现有 stats 格）

- [ ] **Step 5: 前端构建**（npm run build 0 error）→ **Step 6: 双端部署**（dysync 前端 cp+展平+commit+recreate；ASR static 是直接目录无需重启——`Ctrl+F5` 强刷）

- [ ] **Step 7: Commit（两仓各自）**

```bash
cd D:/dysync/dysync.net && git add -A && git commit -m "feat(asr): 前端五态+条件轮询+看板队列格"
cd D:/ASR-For-Dysync && git add -A && git commit -m "feat: 看板队列/转换中计数展示"
```

---

### Task 5: E2E 全链路验证

**Files:** 无代码

- [ ] **Step 1: 提交→五态全程**：删一条已生成视频的 SubtitleSavePath/Msg/AsrTaskId（造「待提交」）→ 观察前端 待提交→排队中→转换中→已生成；ASR 看板 队列N/转换中1
- [ ] **Step 2: 自愈**：转换中 `docker compose restart dysync2026`（注意 restart 不 recreate 镜像不变,本测试无害）→ 恢复后状态续写不丢
- [ ] **Step 3: 404 重报**：`docker exec` 杀 ASR python（队列清空但 dysync 库 TaskId 在）→ watcher 拉起后 dysync 404 路径重报（RetryCount 递增）
- [ ] **Step 4: 幂等回归**：Task 1 Step 7 冒烟已证（dedup=true）
- [ ] **Step 5: 同步回归**：手动同步一轮关注 → 入库秒级完成（无转写阻塞）→ 2 分钟内字幕自动开始
- [ ] **Step 6: 手动+队列让位**：排队中的视频手动「生成字幕」→ 完成后队列写回让位（Path 已有，只清 TaskId）
- [ ] **Step 7: 还原测试数据 + 更新记忆**

---

## Self-Review 结论

- Spec 覆盖：ASR 五项改动(T1)、实体+客户端(T2)、Job+瘦身+注册(T3)、前端两处(T4)、E2E 含自愈/404/幂等/让位(T5)✅；spec「EnsureAsrRunningAsync 复用」在 T3 Step 2 以双探实现（该方法 private——Job 探两次不强拉,拉起由 dysync 现有同步/手动入口的 flag 机制覆盖；若 E2E 发现队列卡死再升级为 public 复用）——记为已知偏差
- 占位符：T4 Step 3/4 标注「grep 定位」但给出定位关键词与零改动判定条件，可执行
- 类型一致性：QueueSubmitAsync 4 元组（T2 定义/T3 消费一致）；BuildSrtContentFrom 适配重载在 T3 内定义于 LocalAsrSubtitleService（T2 文件）；asrTaskStatus 前后端字段名一致
